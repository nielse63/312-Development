
export default {
  // statsForRepo: state => repo => state.stats[repo],
  // repoCount(state) {
  //   return state.user.public_repos;
  // },
  // githubStars(state) {
  //   let count = 0;
  //   state.repos.forEach((repo) => {
  //     count += repo.stargazers;
  //   });
  //   return count;
  // },
  // githubCommits(state) {
  //   const count = 0;
  //   Object.keys(state.stats).forEach((key) => {
  //     console.log(key);
  //     // count += object.commits;
  //   });
  //   return count;
  // },
};
