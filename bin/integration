#!/usr/bin/env bash
set -e

# set default env vars
if [ ! "$NODE_ENV" ]; then
  export NODE_ENV=test
fi

# build on travis
if [ "$TRAVIS" ]; then
  yarn run build
else
  . bin/print-env
  node_modules/.bin/eslint test/integration --fix
fi

node test/integration/runner.js
