#!/usr/bin/env bash
set -e

if ! [ $PORT ]; then
  $PORT=3000
  export $PORT
fi

if ! [ $(command -v http-server) ]; then
  npm i -g http-server
fi

# serve
http-server -p $PORT \
  --proxy $API_HOST \
  --gzip \
  --no-dotfiles \
  dist/
