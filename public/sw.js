var __wpo = {
  "assets": {
    "main": [
      "/dist/../fonts/Brandon_bld.eot?90a61de0680c50299f7b6f662ff5d8ec",
      "/dist/../fonts/Brandon_reg.eot?71d2efe2f1a239837d35608672ac280a",
      "/dist/../fonts/Brandon_bld.ttf?246442f44d26fb1f7a3498aff1cdfca8",
      "/dist/../fonts/Brandon_bld.woff?1d13843717e729e547c34c70989e6375",
      "/dist/../fonts/Brandon_reg.ttf?fdecab1b67d301653660f94fe150efca",
      "/dist/../fonts/Brandon_reg.woff?e65ae2aaad4adb11be846f7213914833",
      "/dist/../fonts/ProximaNova.eot?3ee938ac2658296bae1ed71b29beb804",
      "/dist/../fonts/ProximaNova.ttf?410504d49238e955ba7dc23a7f963021",
      "/dist/../fonts/ProximaNova.woff?03b2a5ecf27db587588625c92d9de92e",
      "/dist/../fonts/ProximaNova.woff2?263ee7dcbdbd37186612c6ba59b3fcfb",
      "/dist/../fonts/ProximaNovaBold.eot?b7639040b92a61eabe14a0cddc69b46e",
      "/dist/../fonts/ProximaNovaBold.ttf?91d3c9f35d738ae967ceadc661b29c2a",
      "/dist/../fonts/ProximaNovaBold.woff?d06f11e7c1aa87d723fc0a0757161928",
      "/dist/../fonts/ProximaNovaBold.woff2?6e11151b09fb25e9cf6637b4f9c07191",
      "/dist/../fonts/ProximaNovaBoldItalic.eot?14f10a22be532a5837e1fbc49f4d8950",
      "/dist/../fonts/ProximaNovaBoldItalic.ttf?2c979a6040243ce1e2cbb24ce8a61416",
      "/dist/../fonts/ProximaNovaBoldItalic.woff?f2e234f727babe0492b9b9866f340987",
      "/dist/../fonts/ProximaNovaBoldItalic.woff2?968fa8834914ee45fa5d5987e8f68151",
      "/dist/../fonts/ProximaNovaItalic.eot?4eb0561d142fb1723f2e04a6960cada4",
      "/dist/../fonts/ProximaNovaItalic.ttf?96b34dd3fa3bf9942d57bb3b67a6f0ba",
      "/dist/../fonts/ProximaNovaItalic.woff?7aa1bac245edae529b27b6e5400ca860",
      "/dist/../fonts/ProximaNovaItalic.woff2?a2c24e3b44c2ae490ec20493cf333095",
      "/dist/../fonts/ProximaNovaLight.eot?faba0f122e078b1f7b17d3b3731cfe62",
      "/dist/../fonts/ProximaNovaLight.ttf?2674a720ecb4a274add69fb109fed474",
      "/dist/../fonts/ProximaNovaLight.woff?6ab1d34acee05851c10f56797d1c398f",
      "/dist/../fonts/ProximaNovaLight.woff2?f1a16d5c4acdbf378356693ff59f6e63",
      "/dist/../fonts/ProximaNovaLightIt.eot?7b751f61069425349ced9005aec7bae0",
      "/dist/../fonts/ProximaNovaLightIt.ttf?dd304b226a05f3cdd09f9f21fc5bad3c",
      "/dist/../fonts/ProximaNovaLightIt.woff?eb7ddf03893e22bfa0b655f740a0fe5c",
      "/dist/../fonts/ProximaNovaLightIt.woff2?b88fd6c563919b70bcb0847c88965cf3",
      "/dist/../fonts/crimsontext-roman-webfont.woff?d58ab0283b9c707cd631cf2019b5f7cc",
      "/dist/../fonts/crimsontext-roman-webfont.woff2?2ecc57bb2583176f40921065cad3816c",
      "/dist/../images/404.jpg?2ba377079c3c50ee274f22310e9b6942",
      "/dist/../images/about.jpg?9edcadc4f2f4e259277ec2d16ab98d39",
      "/dist/../images/contact.jpg?946095ef8f3fae4b6c3d10b839e64a01",
      "/dist/app.js?e84478ef64547e0611fd",
      "/dist/scss.js?e84478ef64547e0611fd",
      "/dist/app.js.map?e84478ef64547e0611fd",
      "/dist/scss.js.map?e84478ef64547e0611fd"
    ],
    "additional": [],
    "optional": []
  },
  "hashesMap": {
    "90a61de0680c50299f7b6f662ff5d8ec": "/dist/../fonts/Brandon_bld.eot?90a61de0680c50299f7b6f662ff5d8ec",
    "71d2efe2f1a239837d35608672ac280a": "/dist/../fonts/Brandon_reg.eot?71d2efe2f1a239837d35608672ac280a",
    "246442f44d26fb1f7a3498aff1cdfca8": "/dist/../fonts/Brandon_bld.ttf?246442f44d26fb1f7a3498aff1cdfca8",
    "1d13843717e729e547c34c70989e6375": "/dist/../fonts/Brandon_bld.woff?1d13843717e729e547c34c70989e6375",
    "fdecab1b67d301653660f94fe150efca": "/dist/../fonts/Brandon_reg.ttf?fdecab1b67d301653660f94fe150efca",
    "e65ae2aaad4adb11be846f7213914833": "/dist/../fonts/Brandon_reg.woff?e65ae2aaad4adb11be846f7213914833",
    "3ee938ac2658296bae1ed71b29beb804": "/dist/../fonts/ProximaNova.eot?3ee938ac2658296bae1ed71b29beb804",
    "410504d49238e955ba7dc23a7f963021": "/dist/../fonts/ProximaNova.ttf?410504d49238e955ba7dc23a7f963021",
    "03b2a5ecf27db587588625c92d9de92e": "/dist/../fonts/ProximaNova.woff?03b2a5ecf27db587588625c92d9de92e",
    "263ee7dcbdbd37186612c6ba59b3fcfb": "/dist/../fonts/ProximaNova.woff2?263ee7dcbdbd37186612c6ba59b3fcfb",
    "b7639040b92a61eabe14a0cddc69b46e": "/dist/../fonts/ProximaNovaBold.eot?b7639040b92a61eabe14a0cddc69b46e",
    "91d3c9f35d738ae967ceadc661b29c2a": "/dist/../fonts/ProximaNovaBold.ttf?91d3c9f35d738ae967ceadc661b29c2a",
    "d06f11e7c1aa87d723fc0a0757161928": "/dist/../fonts/ProximaNovaBold.woff?d06f11e7c1aa87d723fc0a0757161928",
    "6e11151b09fb25e9cf6637b4f9c07191": "/dist/../fonts/ProximaNovaBold.woff2?6e11151b09fb25e9cf6637b4f9c07191",
    "14f10a22be532a5837e1fbc49f4d8950": "/dist/../fonts/ProximaNovaBoldItalic.eot?14f10a22be532a5837e1fbc49f4d8950",
    "2c979a6040243ce1e2cbb24ce8a61416": "/dist/../fonts/ProximaNovaBoldItalic.ttf?2c979a6040243ce1e2cbb24ce8a61416",
    "f2e234f727babe0492b9b9866f340987": "/dist/../fonts/ProximaNovaBoldItalic.woff?f2e234f727babe0492b9b9866f340987",
    "968fa8834914ee45fa5d5987e8f68151": "/dist/../fonts/ProximaNovaBoldItalic.woff2?968fa8834914ee45fa5d5987e8f68151",
    "4eb0561d142fb1723f2e04a6960cada4": "/dist/../fonts/ProximaNovaItalic.eot?4eb0561d142fb1723f2e04a6960cada4",
    "96b34dd3fa3bf9942d57bb3b67a6f0ba": "/dist/../fonts/ProximaNovaItalic.ttf?96b34dd3fa3bf9942d57bb3b67a6f0ba",
    "7aa1bac245edae529b27b6e5400ca860": "/dist/../fonts/ProximaNovaItalic.woff?7aa1bac245edae529b27b6e5400ca860",
    "a2c24e3b44c2ae490ec20493cf333095": "/dist/../fonts/ProximaNovaItalic.woff2?a2c24e3b44c2ae490ec20493cf333095",
    "faba0f122e078b1f7b17d3b3731cfe62": "/dist/../fonts/ProximaNovaLight.eot?faba0f122e078b1f7b17d3b3731cfe62",
    "2674a720ecb4a274add69fb109fed474": "/dist/../fonts/ProximaNovaLight.ttf?2674a720ecb4a274add69fb109fed474",
    "6ab1d34acee05851c10f56797d1c398f": "/dist/../fonts/ProximaNovaLight.woff?6ab1d34acee05851c10f56797d1c398f",
    "f1a16d5c4acdbf378356693ff59f6e63": "/dist/../fonts/ProximaNovaLight.woff2?f1a16d5c4acdbf378356693ff59f6e63",
    "7b751f61069425349ced9005aec7bae0": "/dist/../fonts/ProximaNovaLightIt.eot?7b751f61069425349ced9005aec7bae0",
    "dd304b226a05f3cdd09f9f21fc5bad3c": "/dist/../fonts/ProximaNovaLightIt.ttf?dd304b226a05f3cdd09f9f21fc5bad3c",
    "eb7ddf03893e22bfa0b655f740a0fe5c": "/dist/../fonts/ProximaNovaLightIt.woff?eb7ddf03893e22bfa0b655f740a0fe5c",
    "b88fd6c563919b70bcb0847c88965cf3": "/dist/../fonts/ProximaNovaLightIt.woff2?b88fd6c563919b70bcb0847c88965cf3",
    "d58ab0283b9c707cd631cf2019b5f7cc": "/dist/../fonts/crimsontext-roman-webfont.woff?d58ab0283b9c707cd631cf2019b5f7cc",
    "2ecc57bb2583176f40921065cad3816c": "/dist/../fonts/crimsontext-roman-webfont.woff2?2ecc57bb2583176f40921065cad3816c",
    "2ba377079c3c50ee274f22310e9b6942": "/dist/../images/404.jpg?2ba377079c3c50ee274f22310e9b6942",
    "9edcadc4f2f4e259277ec2d16ab98d39": "/dist/../images/about.jpg?9edcadc4f2f4e259277ec2d16ab98d39",
    "946095ef8f3fae4b6c3d10b839e64a01": "/dist/../images/contact.jpg?946095ef8f3fae4b6c3d10b839e64a01",
    "d36bd27798a415519e90c9c52c983d77": "/dist/app.js?e84478ef64547e0611fd",
    "c3a69a947c85a79da30a713beb1ff359": "/dist/scss.js?e84478ef64547e0611fd",
    "88aebb4cfd44417b957f741182bb08ab": "/dist/app.js.map?e84478ef64547e0611fd",
    "d89111cb81e7eb765169c1289c02c444": "/dist/scss.js.map?e84478ef64547e0611fd"
  },
  "strategy": "all",
  "version": "7/29/2016, 12:55:29 AM",
  "name": "webpack-offline",
  "relativePaths": false
};

/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};

/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {

/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;

/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};

/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);

/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;

/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}


/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;

/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;

/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/dist/";

/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/*!************************************************************************************************************!*\
  !*** ./~/offline-plugin/lib/misc/sw-loader.js?{"data_var_name":"__wpo"}!./~/offline-plugin/empty-entry.js ***!
  \************************************************************************************************************/
/***/ function(module, exports, __webpack_require__) {

	
	      'use strict';

	if (false) {
	  var DEBUG = false;
	}

	function WebpackServiceWorker(params) {
	  var strategy = params.strategy;
	  var assets = params.assets;
	  var hashesMap = params.hashesMap;

	  // Not used yet
	  // const alwaysRevalidate = params.alwaysRevalidate;
	  // const ignoreSearch = params.ignoreSearch;
	  // const preferOnline = params.preferOnline;

	  var tagMap = {
	    all: params.version,
	    changed: params.version
	  };

	  var CACHE_PREFIX = params.name;
	  var CACHE_TAG = tagMap[strategy];
	  var CACHE_NAME = CACHE_PREFIX + ':' + CACHE_TAG;

	  var STORED_DATA_KEY = '__offline_webpack__data';

	  mapAssets();

	  var allAssets = [].concat(assets.main, assets.additional, assets.optional);
	  var navigateFallbackURL = params.navigateFallbackURL;

	  self.addEventListener('install', function (event) {
	    console.log('[SW]:', 'Install event');

	    var installing = undefined;

	    if (strategy === 'changed') {
	      installing = cacheChanged('main');
	    } else {
	      installing = cacheAssets('main');
	    }

	    event.waitUntil(installing);
	  });

	  self.addEventListener('activate', function (event) {
	    console.log('[SW]:', 'Activate event');

	    var activation = cacheAdditional();

	    // Delete all assets which name starts with CACHE_PREFIX and
	    // is not current cache (CACHE_NAME)
	    activation = activation.then(storeCacheData);
	    activation = activation.then(deleteObsolete);
	    activation = activation.then(function () {
	      if (self.clients && self.clients.claim) {
	        return self.clients.claim();
	      }
	    });

	    event.waitUntil(activation);
	  });

	  function cacheAdditional() {
	    if (!assets.additional.length) {
	      return Promise.resolve();
	    }

	    if (true) {
	      console.log('[SW]:', 'Caching additional');
	    }

	    var operation = undefined;

	    if (strategy === 'changed') {
	      operation = cacheChanged('additional');
	    } else {
	      operation = cacheAssets('additional');
	    }

	    // Ignore fail of `additional` cache section
	    return operation['catch'](function (e) {
	      console.error('[SW]:', 'Cache section `additional` failed to load');
	    });
	  }

	  function cacheAssets(section) {
	    var batch = assets[section];

	    return caches.open(CACHE_NAME).then(function (cache) {
	      return addAllNormalized(cache, batch, {
	        bust: params.version
	      });
	    }).then(function () {
	      logGroup('Cached assets: ' + section, batch);
	    })['catch'](function (e) {
	      console.error(e);
	      throw e;
	    });
	  }

	  function cacheChanged(section) {
	    return getLastCache().then(function (args) {
	      if (!args) {
	        return cacheAssets(section);
	      }

	      var lastCache = args[0];
	      var lastKeys = args[1];
	      var lastData = args[2];

	      var lastMap = lastData.hashmap;
	      var lastVersion = lastData.version;

	      if (!lastData.hashmap || lastVersion === params.version) {
	        return cacheAssets(section);
	      }

	      var lastHashedAssets = Object.keys(lastMap).map(function (hash) {
	        return lastMap[hash];
	      });

	      var lastUrls = lastKeys.map(function (req) {
	        var url = new URL(req.url);
	        url.search = '';

	        return url.toString();
	      });

	      var sectionAssets = assets[section];
	      var moved = [];
	      var changed = sectionAssets.filter(function (url) {
	        if (lastUrls.indexOf(url) === -1 || lastHashedAssets.indexOf(url) === -1) {
	          return true;
	        }

	        return false;
	      });

	      Object.keys(hashesMap).forEach(function (hash) {
	        var asset = hashesMap[hash];

	        // Return if not in sectionAssets or in changed or moved array
	        if (sectionAssets.indexOf(asset) === -1 || changed.indexOf(asset) !== -1 || moved.indexOf(asset) !== -1) return;

	        var lastAsset = lastMap[hash];

	        if (lastAsset && lastUrls.indexOf(lastAsset) !== -1) {
	          moved.push([lastAsset, asset]);
	        } else {
	          changed.push(asset);
	        }
	      });

	      logGroup('Changed assets: ' + section, changed);
	      logGroup('Moved assets: ' + section, moved);

	      var movedResponses = Promise.all(moved.map(function (pair) {
	        return lastCache.match(pair[0]).then(function (response) {
	          return [pair[1], response];
	        });
	      }));

	      return caches.open(CACHE_NAME).then(function (cache) {
	        var move = movedResponses.then(function (responses) {
	          return Promise.all(responses.map(function (pair) {
	            return cache.put(pair[0], pair[1]);
	          }));
	        });

	        return Promise.all([move, addAllNormalized(cache, changed, {
	          bust: params.version
	        })]);
	      });
	    });
	  }

	  function deleteObsolete() {
	    return caches.keys().then(function (keys) {
	      var all = keys.map(function (key) {
	        if (key.indexOf(CACHE_PREFIX) !== 0 || key.indexOf(CACHE_NAME) === 0) return;

	        console.log('[SW]:', 'Delete cache:', key);
	        return caches['delete'](key);
	      });

	      return Promise.all(all);
	    });
	  }

	  function getLastCache() {
	    return caches.keys().then(function (keys) {
	      var index = keys.length;
	      var key = undefined;

	      while (index--) {
	        key = keys[index];

	        if (key.indexOf(CACHE_PREFIX) === 0) {
	          break;
	        }
	      }

	      if (!key) return;

	      var cache = undefined;

	      return caches.open(key).then(function (_cache) {
	        cache = _cache;
	        return _cache.match(new URL(STORED_DATA_KEY, location).toString());
	      }).then(function (response) {
	        if (!response) return;

	        return Promise.all([cache, cache.keys(), response.json()]);
	      });
	    });
	  }

	  function storeCacheData() {
	    return caches.open(CACHE_NAME).then(function (cache) {
	      var data = new Response(JSON.stringify({
	        version: params.version,
	        hashmap: hashesMap
	      }));

	      return cache.put(new URL(STORED_DATA_KEY, location).toString(), data);
	    });
	  }

	  self.addEventListener('fetch', function (event) {
	    var url = new URL(event.request.url);
	    url.search = '';
	    var urlString = url.toString();

	    // Match only GET and known caches, otherwise just ignore request
	    if (event.request.method !== 'GET' || allAssets.indexOf(urlString) === -1) {
	      if (navigateFallbackURL && isNavigateRequest(event.request)) {
	        event.respondWith(handleNavigateFallback(fetch(event.request)));

	        return;
	      }

	      // Fix for https://twitter.com/wanderview/status/696819243262873600
	      if (url.origin !== location.origin && navigator.userAgent.indexOf('Firefox/44.') !== -1) {
	        event.respondWith(fetch(event.request));
	      }

	      return;
	    }

	    var resource = cachesMatch(urlString, CACHE_NAME).then(function (response) {
	      if (response) {
	        if (true) {
	          console.log('[SW]:', 'URL [' + urlString + '] from cache');
	        }

	        return response;
	      }

	      // Load and cache known assets
	      var fetching = fetch(event.request).then(function (response) {
	        if (!response || !response.ok) {
	          if (true) {
	            console.log('[SW]:', 'URL [' + urlString + '] wrong response: [' + response.status + '] ' + response.type);
	          }

	          return response;
	        }

	        if (true) {
	          console.log('[SW]:', 'URL [' + urlString + '] fetched');
	        }

	        var responseClone = response.clone();

	        caches.open(CACHE_NAME).then(function (cache) {
	          return cache.put(urlString, responseClone);
	        }).then(function () {
	          console.log('[SW]:', 'Cache asset: ' + urlString);
	        });

	        return response;
	      });

	      if (navigateFallbackURL && isNavigateRequest(event.request)) {
	        return handleNavigateFallback(fetching);
	      }

	      return fetching;
	    });

	    event.respondWith(resource);
	  });

	  self.addEventListener('message', function (e) {
	    var data = e.data;
	    if (!data) return;

	    switch (data.action) {
	      case 'skipWaiting':
	        {
	          if (self.skipWaiting) self.skipWaiting();
	        }break;
	    }
	  });

	  function handleNavigateFallback(fetching) {
	    return fetching['catch'](function () {}).then(function (response) {
	      if (!response || !response.ok) {
	        if (true) {
	          console.log('[SW]:', 'Loading navigation fallback [' + navigateFallbackURL + '] from cache');
	        }

	        return cachesMatch(navigateFallbackURL, CACHE_NAME);
	      }

	      return response;
	    });
	  }

	  function mapAssets() {
	    Object.keys(assets).forEach(function (key) {
	      assets[key] = assets[key].map(function (path) {
	        var url = new URL(path, location);
	        url.search = '';

	        return url.toString();
	      });
	    });

	    hashesMap = Object.keys(hashesMap).reduce(function (result, hash) {
	      var url = new URL(hashesMap[hash], location);
	      url.search = '';

	      result[hash] = url.toString();
	      return result;
	    }, {});
	  }
	}

	function addAllNormalized(cache, requests, options) {
	  var bustValue = options && options.bust;

	  return Promise.all(requests.map(function (request) {
	    if (bustValue) {
	      request = applyCacheBust(request, bustValue);
	    }

	    return fetch(request);
	  })).then(function (responses) {
	    if (responses.some(function (response) {
	      return !response.ok;
	    })) {
	      return Promise.reject(new Error('Wrong response status'));
	    }

	    var addAll = responses.map(function (response, i) {
	      return cache.put(requests[i], response);
	    });

	    return Promise.all(addAll);
	  });
	}

	function cachesMatch(request, cacheName) {
	  return caches.match(request, {
	    cacheName: cacheName
	  })
	  // Return void if error happened (cache not found)
	  ['catch'](function () {});
	}

	function applyCacheBust(asset, key) {
	  var hasQuery = asset.indexOf('?') !== -1;
	  return asset + (hasQuery ? '&' : '?') + '__uncache=' + encodeURIComponent(key);
	}

	function getClientsURLs() {
	  if (!self.clients) {
	    return Promise.resolve([]);
	  }

	  return self.clients.matchAll({
	    includeUncontrolled: true
	  }).then(function (clients) {
	    if (!clients.length) return [];

	    var result = [];

	    clients.forEach(function (client) {
	      var url = new URL(client.url);
	      url.search = '';
	      url.hash = '';
	      var urlString = url.toString();

	      if (!result.length || result.indexOf(urlString) === -1) {
	        result.push(urlString);
	      }
	    });

	    return result;
	  });
	}

	function isNavigateRequest(request) {
	  return request.mode === 'navigate' || request.headers.get('Upgrade-Insecure-Requests') || (request.headers.get('Accept') || '').indexOf('text/html') !== -1;
	}

	function logGroup(title, assets) {
	  console.groupCollapsed('[SW]:', title);

	  assets.forEach(function (asset) {
	    console.log('Asset:', asset);
	  });

	  console.groupEnd();
	}
	      __webpack_require__(/*! !./lib/misc/sw-polyfill.js */ 2)
	      WebpackServiceWorker(__wpo);
	      module.exports = __webpack_require__(/*! ./empty-entry.js */ 1)
	    

/***/ },
/* 1 */
/*!*****************************************!*\
  !*** ./~/offline-plugin/empty-entry.js ***!
  \*****************************************/
/***/ function(module, exports) {

	

/***/ },
/* 2 */
/*!**************************************************!*\
  !*** ./~/offline-plugin/lib/misc/sw-polyfill.js ***!
  \**************************************************/
/***/ function(module, exports) {

	"use strict";

/***/ }
/******/ ]);