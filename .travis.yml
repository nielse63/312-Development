
language: node_js
node_js:
  - "node"
sudo: false
cache:
  yarn: true
  directories:
    - $HOME/.npm
    - node_modules
dist: trusty
addons:
  chrome: stable
before_install:
  - npm config set spin false

env:
  global:
    - JOBS=1
    - NODE_ENV=test
#   matrix:
#     - TEST_COMMAND="yarn run unit"
#     - TEST_COMMAND="yarn run integration"

# script:
#   - (eval "$TEST_COMMAND")
jobs:
  include:
    - script: yarn run unit
    - script: yarn run integration
