---
sudo: false
language: node_js
node_js:
  - v7
  - v6
  - v5
  - v4
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH=$HOME/.yarn/bin:$PATH
cache:
  directories:
    - $HOME/.cache/yarn
    - node_modules
